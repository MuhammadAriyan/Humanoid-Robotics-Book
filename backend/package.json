{
  "name": "fubuni-chat-backend",
  "version": "1.0.0",
  "description": "FastAPI backend for Fubuni Chat system",
  "main": "app/main.py",
  "scripts": {
    "start": "uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "test": "python -m pytest tests/ -v"
  },
  "dependencies": {
    "fastapi": "^0.115.6",
    "uvicorn": "^0.34.0",
    "asyncpg": "^0.30.0",
    "sqlmodel": "^0.0.22",
    "pydantic": "^2.10.3",
    "python-dotenv": "^1.0.1",
    "openai": "^1.66.2",
    "httpx": "^0.28.1",
    "openai-agents": "^0.0.4"
  },
  "engines": {
    "python": "3.11"
  }
}